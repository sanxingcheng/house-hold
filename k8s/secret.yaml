# 敏感配置请用 Secret 管理，部署前用实际值替换 base64 或使用 kubectl create secret
apiVersion: v1
kind: Secret
metadata:
  name: house-hold-secret
  namespace: house-hold
type: Opaque
stringData:
  MYSQL_PASSWORD: "Admin@123"   # 生产环境务必修改
